{
  "name": "@airways/health-monitor",
  "version": "1.0.0",
  "description": "Health monitoring service — SQL integrity checks + LLM cross-signal correlation for airways.gg",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@airways/common": "*",
    "@airways/database": "*",
    "@airways/telegram": "*",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  }
}
