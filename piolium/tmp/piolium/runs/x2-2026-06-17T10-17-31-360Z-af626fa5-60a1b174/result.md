Longshot anchor af626fa5 (apps/web/src/routes/stats/lib/transforms.ts) complete. Drafts: 0.

**Analysis summary:** The anchor file is a pure data transformation utility with 18 exported functions — all stateless, no I/O, no network, no file access. The full data pipeline was traced: URL query params → `+page.server.ts` (validated/parsed) → `queries.ts` (Drizzle ORM with exclusively parameterized SQL) → typed result objects → transforms (pure math/formatting) → Svelte templates (auto-escaped). No SQL injection (all parameterized), no XSS (`{@html}` never used), no SSRF, no command injection, no prototype pollution. The `emptyState` function that returns raw HTML strings is dead code (defined but never invoked in the template). `cellBg` output is always clamped to numeric range 0–100 and used only in CSS `style` attributes. This file is clean.
